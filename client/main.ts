interface User {
    id: number
    email: string
    firstName: string
    lastName: string
}

const user: User = {
    id: 1,
    email: 'test',
    firstName: 'philip',
    lastName: 'braunen'
}

console.log(user)
